<%= bootstrap_form_with(model: article, local: true, label_errors: true, inline_errors: true) do |form| %>

    <%= form.text_field :title, label: "Title", required: true %>
    <%= form.text_area :description, label: "Description", required: true %>
    <%= form.submit %>

<% end %>
