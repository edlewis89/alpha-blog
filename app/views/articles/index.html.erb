
  <h1 class="text-center mt-4">Articles Listings</h1>
  <div class="flicker_pagination">
    <%= will_paginate @articles, container: false %>
  </div>
    <%= render 'article', obj: @articles %>
  <div class="flicker_pagination mb-4">
    <%= will_paginate @articles, container: false %>
  </div>
